const ValueRules = [
    'between',
    'contains',
    'min',
    'max',
    'regex',
    'date_equals_to',
    'date_greater_than',
    'date_greater_than_equals_to',
    'date_less_than',
    'date_less_than_equals_to'
] as const;

export default ValueRules