const KeyRules = [
    'accepted_if',
    'declined_if',
    'required_if',
    'required_if_accepted',
    'required_if_any',
    'required_if_declined',
    'required_if_not',
    'required_if_not_any',
    'same',
] as const;

export default KeyRules